<?php
//
importar conn.php

// Consulta SQL para obtener los datos de la tabla
$sql = "SELECT * FROM articulos_universitarios";
$result = $conn->query($sql);

if ($result->num_rows > 0) {
    // Imprimir los datos en formato HTML
    while ($row = $result->fetch_assoc()) {
        // Aquí puedes generar el HTML para cada tarjeta usando los datos de la base de datos
        echo '<div class="causes-item d-flex flex-column bg-white border-top border-5 border-primary rounded-top overflow-hidden h-100">';
        echo '<div class="text-center p-4 pt-0">';
        echo '<div class="d-inline-block bg-primary text-white rounded-bottom fs-5 pb-1 px-3 mb-4">';
        echo '<small>' . $row['categoria'] . '</small>';
        echo '</div>';
        echo '<h5 class="mb-3">' . $row['titulo'] . '</h5>';
        echo '<p>' . $row['descripcion'] . '</p>';
        echo '</div>';
        echo '<div class="position-relative mt-auto">';
        echo '<img class="img-fluid" src="' . $row['imagen_url'] . '" alt="">';
        echo '<div class="causes-overlay">';
        echo '<a class="btn btn-outline-primary" href="' . $row['link_url'] . '">';
        echo 'Leer más';
        echo '<div class="d-inline-flex btn-sm-square bg-primary text-white rounded-circle ms-2">';
        echo '<i class="fa fa-arrow-right"></i>';
        echo '</div>';
        echo '</a>';
        echo '</div>';
        echo '</div>';
        echo '</div>';
    }
} else {
    echo "0 resultados";
}

$conn->close();
?>
